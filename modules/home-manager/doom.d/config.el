(setq doom-font (font-spec :family "Fira Code" :size 22.0))
(setq display-line-numbers-type 'relative)
(setq doom-theme 'doom-one)
;; todo: use evil mode command as second args, rather than mapping keys to keys
;; todo: center after scroll down
;; (with-eval-after-load 'evil-maps
;;   (define-key evil-normal-state-map (kbd "C-d") 'evil-scroll-down)
;;   (define-key evil-normal-state-map (kbd "C-d") 'evil-scroll-down))
